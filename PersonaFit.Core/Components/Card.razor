
<div class="card-container">
    
    <div class="card-inset">
        <div style="font-size: 12px; text-align: center;">
            This is where your workouts will show.
        </div>
    </div>

</div>

<style>
    .card-container {
        width: 100%;
        height: 100%;
        margin: 5px;
        /* 1. GUNMETAL GREY GLASS BASE */
        /* Uses a semi-transparent slate-grey instead of deep black */
        background: #333;
        );
        /* 2. THE GLASS BLUR EFFECT */
        /* Blurs the spinning neon lights behind the card */
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px); /* Safari support */
        /* 3. METALLIC INNER REFLECTION */
        /* A tiny white border reflection on the inside lip of the card */
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 5px;
        color: #fff;
        z-index: 2;
    }

    .card-inset {
        width: 100%;
        height: 50px;
        margin: 10px;
        color: white;
        margin: 10px;
    }
</style>

@code {

}
